# 変更の記録

**新しいものが上。いま見るべきものは先頭。**
採否の基準は [CRITERIA.md](CRITERIA.md)、手法の説明は [README.md](README.md)。
ここは「いつ何をして、数字がどう動いたか」だけを、新しい順に置く。

**指標を書いてよいのはこのファイルだけ。** 他の md は、ここへリンクする。
同じ数字を2か所に書くと、片方が必ず古くなる。

---

## 現在地

すべて**デモと同じ処理解像度**（画素数 180×320 固定・縦横比は入力のまま）。

| | mIoU | 適合率 | 再現率 |
|---|---|---|---|
| **回帰セット 27枚** | **0.875** | **0.973** | **0.899** |
| val 335枚 | 0.794 | 0.922 | 0.846 |
| train 3901枚 | 0.781 | 0.951 | 0.813 |

回帰セットは `img/` 由来19枚＋「青空にはっきりした白い雲」8枚。
**19枚時代の 0.935 とは比較できない**（セットが変わった）。

速度はフレーム全体で **iPhone SE (第3世代) 7.5ms**、M 系デスクトップ 約9ms
（デスクトップの分離処理のみは 3.2ms）。**30fps の予算 33ms に対して4倍以上の余裕**があり、
要件の核心は実測で満たされている。

回帰セットの弱点は動画の後半（カメラがパンして構図が変わる区間）。
残る誤検出は街灯のランプヘッドと光芒で、直線ではないため今の手では取れない。

---

## `minSeedCount` も面積比にする — 採用

残っていたもう1つの絶対数。`minSeedCount: 50` → `minSeedRatio: 0.003`
（較正時の 50/16640。粗面積 14400 なら43画素）。

回帰セットと val は**出力が1画素も変わらない**。val ではしきい値を 1 から 100 まで
振っても発火しないので、そもそも効いていない。一方 train では5枚で発火しており、
いずれも空が小さく候補が43〜49個の構図。43に下がることで、この5枚が
**全0マスクから救われる**（IoU 0.000 → 0.008〜0.040、適合率はいずれも 1.000）。
train の完全失敗が 4.3% → 4.2%、集計指標は不変。

val だけを見て「一度も発火しない」と判断しかけた。**全セットで差分を取ること。**

## しきい値を粗画像の面積比にする — 採用

`modeMinCount: 20`（絶対数）を `modeMinRatio: 0.0012`（粗画像の面積比）に。
較正時の粗面積 16640 での 20 を比にしたもの。

**絶対数のまま解像度を変えたせいで、必達要件が黙って壊れていた。**
`baa81fd` で処理解像度を入力の縦横比に合わせた結果、粗面積が 16640 → 14400 になり、
第2モードの候補が16〜19個でしきい値 20 に届かなくなった。
`1b8d1d161bf4396b`（青空にはっきりした白い雲）が **0.864 → 0.559**。
デモ側は最初からこの解像度なので、実機ではずっとこうなっていた。

| | 前 | 後 |
|---|---|---|
| 回帰セット27枚 mIoU | 0.864 | **0.875** |
| 回帰セット27枚 適合率 | 0.973 | 0.973（不変） |
| 回帰セット27枚 再現率 | 0.888 | **0.899** |
| val mIoU / 適合率 | 0.793 / 0.925 | 0.794 / 0.922 |
| train mIoU / 適合率 | 0.780 / 0.951 | 0.781 / 0.951 |

回帰セットは1枚改善・0枚悪化。`1b8d1d161bf4396b` が 0.559 → **0.854**。

**代償を1枚呑んでいる。** val の `58b51ee683937255` が 0.916 → 0.413（適合率 −0.560）。
曇天の白い空から白い壁づたいにモードが歩いて降りる。val の適合率 −0.003 は
ほぼこの1枚が作っている。歩ける距離に上限を付ければ止まるが、それは雲に届かなく
なるので却下済み。**必達か白い壁かのトレードオフ**で、必達を取ると決めた。
根拠画像: `tools/out/preview/04_面積比_白い壁を飲む代償.png`

## 回帰セットに「青空にはっきりした白い雲」を8枚足す — 19枚 → 27枚

必達要件なのに `img/` の素材にこの構図が無く、**指標に出ないので守れていなかった**。
選定は構図だけで行い、現手法の出来は一切見ていない（自分の変更が良く見える
テストを作らないため）。条件は、正解ラベルの空領域のうち青空らしい画素と
雲らしい画素がそれぞれ15%以上、かつ空が画面の15%以上。552枚が該当し、
そのうち都市・建物が写っているものを採った。

同じコードのまま指標は 0.935 → 0.864 に下がる。劣化ではなく、
**守るべき構図が指標に載った**ということ。多モードを切ると 0.818 まで落ちる。

## 実機モバイルで計測 — 要件を満たすことを確認

**iPhone SE (第3世代) でフレーム全体 7.5ms/frame。** 30fps の予算 33ms に対して4倍以上の余裕。
初期計画の見積もりは「スカラー JS はモバイルで4〜8倍遅いので 12〜23ms」だったが、
実際はそれより良い。表示解像度が小さいぶん `getImageData` の読み戻しと合成が軽いためと見られる
（デスクトップのフレーム全体 約9ms のうち、分離処理は 3.2ms しかない）。

**これで初期計画の最大の未確認事項が閉じた。** 速度を理由に手法を削る必要はない。
以後は速度より品質（適合率）に予算を使ってよい。

## `e86b927` tools/out の置き場所を規則にする — 保守

指標に影響なし。実験の使い捨て出力が split ディレクトリに溜まり、
**正式な `.mask` が一番古く使い捨てが一番新しい**逆転が起きていた。
比較画像は2世代前の結果を映していた。置いてよい拡張子を4種に限り、
`compare.py` はマスクが `dist/skyMask.js` より古ければ止まるようにした。

## `fc2491b` 人工物の直線をマスクから差し引く — 採用

勾配 `|dx|+|dy| > 20` を2値化し、縦横それぞれ半径6のオープニングで
**長い直線だけを残す**。検出された直線の 85.8% が建物の上にあり、雲の縁は 3.2%。
局所σでは雲の縁と建物の縁が分けられないので、独立した手がかりになる。

| | 前 | 後 |
|---|---|---|
| 回帰セット mIoU | 0.928 | **0.935** |
| 回帰セット 適合率 | 0.963 | **0.972** |
| val mIoU / 適合率 | 0.792 / 0.923 | 0.793 / 0.925 |
| train 適合率 | 0.949 | 0.951 |

コストは +0.02〜0.06ms。パン後半の誤検出が 360〜410px → 161〜217px。

**引く位置が決定的だった。** 判定の直後に引くと出力が一切変わらない
（クロージングが細い切れ込みを埋め戻し、`fillHoles` が囲まれた穴を塞ぐ）。
`fillHoles` の**後**に引いて初めて効く。
根拠画像: `tools/out/preview/03_採用_人工物の直線を差し引く.png`

### 却下: エッジを太らせる

直線を太らせて壁を厚くする案。val で改善42枚・悪化50枚、最悪 −0.411。
細い空の筋を潰すため。半径6・2方向・勾配20 のままが最良。

## `baa81fd` 評価の解像度をデモに合わせる — 測定条件の修正

手法の変更ではない。`tools/prep.py` が長辺320固定のままで、
**デモと違う解像度で採点していた**。デモと同じ画素数一定の規則に統一。
これ以前の数字は現在と直接比較できない。再基準化後: 回帰 0.928 / 0.963 / 0.961。

## `90db0dd` morphSquare を移動和にする — 採用（速度のみ）

窓の合計を累積和の差で取り、半径に依存しない O(1)/画素に。
**4255枚で出力が1画素も変わらず**、val 6.18 → 3.94ms（−36%）、
回帰セット 4.77 → 3.02ms（−37%）。

## `ec7561e` 処理解像度を入力の縦横比に合わせる — 採用

固定 180×320 だと横長ソースが潰れ、ビル間の細い隙間が判定解像度で消えていた。
画素数だけを固定し、縦横比は入力のまま。

| cloudy01 | 前 | 後 |
|---|---|---|
| IoU | 0.950 | **0.989** |
| 再現率 | 0.958 | **0.998** |

しかも速くなる。根拠画像: `tools/out/preview/02_採用_処理解像度を縦横比に合わせる.png`

## `ee3ede3` シード関数を1本に統合、minAreaRatio を外す — 整理

`estimateSkySeed` と `nextSkySeed` を `skySeedIn` 1本に。
`minAreaRatio` は一度も効いていなかったので削除。**4255枚で出力が変わらない**ことを確認。

## `7ee5db1` 採否の基準を CRITERIA.md に固定する

都度ブレるのを止めるため。

## `85e4a0d` 空を複数のモードで表せるようにする — 採用

**必達要件「青空にはっきりした白い雲」を解いた変更。**
青空（輝度113 / 青優勢度0.37）と雲（輝度189 / 0.07）は1組の代表値で囲えない。
空と判った領域の**縁から**次のモードを拾って足す。縁に接していることを求めるのが肝で、
これがないと地続きでない明るい面（白い壁）を拾う。実測3回で収束。

データセット mIoU +0.032、回帰セット 3枚改善・0枚悪化、適合率 −0.001。
`1b8d1d161bf4396b` が 0.599 → **0.871**（正解ラベルを使ったオラクルと同値）。

### 却下: モードの探索とマスク構築を分ける

探索のたびにマスクを作り直すのは冗長に見えるが、分けると回帰セットが 0.928 → 0.911。

### 却下: 領域成長（大枠なし）

データセット mIoU +0.070 と大きく改善するが、回帰セットの適合率が 0.964 → 0.839、
薄暮の f140 が 0.264 まで崩れる。**絶対値の大枠を1倍で残すことが必須。**
成長の上限を1倍より広げる案も同様に却下。

### 却下: σ の障壁（ざらついた画素を越えて成長しない）

「青空にはっきりした雲」を越えられなくなるため、必達要件と両立しない。

## `3da20ff` 境界帯の判定にヒステリシスを入れる — 採用

境界帯での覆しは **非空→空 が 81.8% 正しく、空→非空 は 38.5% しか正しくない**。
空を削る側にだけ `keepTol` 倍の強い証拠を要求する。

mIoU 0.753 → 0.754 とほぼ動かないが、**誤って削られた空の割合が 61.5% → 0**。
**指標に出ない変更の代表例。**別の数字を作って測らなければ効果が見えない。
根拠画像: `tools/out/preview/01_採用_境界帯のヒステリシス.png`

## `4814b79` img/ の素材にも skyseg のラベルを起こして回帰セットにする

データセットの平均は「空が大きく写った易しい画像」に支配されるため、
**このアプリが実際に映すシーンでの劣化を別に見る**必要がある。以降これを最優先で見る。
根拠画像: `tools/out/preview/00_ラベルの品質確認.png`

### 却下: 定数の再較正（`vTol` 60 等）

データセット mIoU +0.025。しかし回帰セットの適合率が 0.926 → 0.867 で、
曇天でビル本体を丸ごと飲む。**mIoU 単独で較正してはいけない**という最初の実例。

### 却下: シードの再推定 / 行ごとのシード

再推定はデータセット mIoU +0.015 だが、f240 で壁面とポールを空と判定（1514px）。
行ごとのシードはオラクルの上限がグローバル最適より低く、着手前に却下。

## `1c466cc` 滑らかさの判定を相対σにする — 採用

局所σを輝度で割る。テクスチャの振幅が明るさに比例するため、
絶対値で切ると明るい空ほど不利になる。青優勢度を `(B-R)/(B+R)` の比にしているのと同じ理由。

データセット 適合率 +0.003 / 再現率 +0.005。回帰セットは出力変化 最大301px、劣化なし。

## それ以前

- `93958da` 計測変数のリネーム漏れで描画が止まっていたのを修正
- `0f20a37` 計測区間をフレーム全体へ広げる（`getImageData` の同期読み戻しが分離処理より高い）
- `869c46d` `c16d07f` 怪獣素材を再配布可能なもの（CC0）へ差し替え
- `601ec49` GitHub Pages を Actions でビルドして配信
- `6e7ec8c` 初期実装。上端シードからの距離で判定する方式、2段パイプライン
