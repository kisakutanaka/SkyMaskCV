# 変更の採否を決める基準

アルゴリズムを変えるたびに判断がブレると、良くなったのか悪くなったのか分からなくなる。
ここに基準を固定しておく。**数字を見る前に採否を決めない。**

---

## 1. 何を最優先で見るか

優先順位は上から。

1. **回帰セット（`img/` の19枚）の適合率** — このアプリが実際に映す被写体での誤検出
2. **回帰セットの再現率**
3. **val / train（Open Images）の mIoU・適合率・再現率**
4. 速度

### 適合率を再現率より重く見る

**建物が空になる誤りは、空を取りこぼす誤りより遥かに目立つ。**
合成モードでは前者は画が壊れ、後者は怪獣が少し隠れるだけで済む。
同じ 0.01 でも、適合率の 0.01 は再現率の 0.01 より重い。

### mIoU 単独で採否を決めない

データセットの平均は「空が大きく写った易しい画像」に支配されるため、
**判定を緩めれば mIoU は上がる**。適合率と再現率を必ず分けて見る。

### データセットより回帰セットを優先する

データセットで改善しても、このアプリが狙うシーン（都市・薄暮・曇天）で
劣化していれば採用しない。逆は検討の余地がある。

---

## 2. 採用の条件

すべて満たすこと。

- 回帰セットで **適合率が 0.005 を超えて下がらない**
- 回帰セットで **IoU が明確に悪化する画像が出ない**（±0.01 を超える悪化が0枚）
- val か train のどちらかで改善する
- フレーム全体が 33ms を超えない（30fps の予算）

## 3. 却下の条件

どれか1つでも該当したら却下。数字が良くても却下する。

- 回帰セットのどれかのシーンで **建物・壁面が空と判定される**
- 回帰セットの適合率が 0.01 以上下がる
- 上限（オラクル）を測った結果、伸びしろが **+0.02 未満**

---

## 4. 指標に出ない変更をどう扱うか

境界の滑らかさや孤立画素は、面積が小さいので IoU にほとんど出ない。
**「mIoU が動かないから効果がない」と判断してはいけない。**

その場合は**狙った現象に対応する数字を別に作って測る**。

| 変更 | IoU での見え方 | 実際に測った数字 |
|---|---|---|
| 境界帯のヒステリシス | mIoU 0.753 → 0.754（ほぼ不変） | 誤って削られた空の割合 61.5% → 0 |
| `despeckle` 2回 | 全く動かない | 孤立画素 2315個 → 12個（val） |
| `bandRadius` 4 | — | 境界の未精緻化 33-47% → 3-8% |

数字が作れないときだけ目視にする。その場合も**比較画像を作って並べる**
（`tools/compare.py`）。記憶や印象で比べない。

---

## 5. 実装の前に上限を測る

「この方向はどこまで行けるのか」を、正解ラベルを使ったオラクルで先に測る。
上限が小さければ着手しない。これで2回、無駄な実装を回避できた。

| 方向 | オラクルの上限 | 判断 |
|---|---|---|
| シードの改善 | mIoU +0.035 | 大半は1回の再シードで消化済み → 着手せず |
| 行ごとのシード | グローバル最適より**低い** | 却下 |
| しきい値の画像別最適化 | **+0.111** | 着手 → 成長型・多モードへ |
| 第2モードの追加 | +0.038、適合率は不変 | 着手 → 採用 |

---

## 6. 決着済みの方針（蒸し返さない）

### 必達

- **青空にはっきりした白い雲** — 多モードで対応済み。壊す変更は入れない

### 許容すると決めたもの

- **人工物で分断されて上端と繋がらない空** — 取れなくてよい。
  評価では正解ラベルを「上端に連結した成分のみ」に絞ってよい
- **空と構造物の境界が元々曖昧な構図**（霞んだ街並み、空を映す青いガラスのビル）
  — モードが構造物側へ歩いて飲み込むが、上限を付けると雲まで届かなくなるため許容
- **動画のフレーム間のちらつき** — 時間方向の平滑化は未実装

---

## 7. 過去の判断（前例）

**採否の記録は [LOG.md](LOG.md) に新しい順で置いてある。**
案を思いついたら、着手する前にそこを検索すること。

前例から得た教訓を1つだけここに書いておく。
**却下した3件は、データセットの数字だけ見ると3件とも採用に見えた。**
定数の再較正は mIoU +0.025、シードの再推定は +0.015、領域成長は +0.070。
回帰セットの適合率を見て初めて却下できた（それぞれ 0.926→0.867、
f240 で壁面を1514px、0.964→0.839）。

---

## 8. 測り方

```sh
# データセット
python3 tools/prep.py val tools/out/val
python3 tools/gt.py   val tools/out/val
node    tools/eval.mjs     tools/out/val

# 回帰セット（必ず見る）
node tools/eval.mjs tools/out/scenes

# 比較画像（目視が必要なとき）
node    tools/masks.mjs  tools/out/val
python3 tools/compare.py tools/out/val tools/out/compare/val
```

### 出力の置き場所

```
tools/out/
  val/ train/ scenes/   .bin .gt .mask manifest.json の4種だけ
  compare/{val,train,scenes}/
  steps/                パイプラインを1段ずつ開いた解説ページ（tools/steps.py）
  preview/              採否の根拠になった画像だけ。番号は採用の時系列
  preview/old/          途中経過・却下した手法。見なくてよい
```

- **split ディレクトリに置いてよいのは上の4種だけ。** 作るのは
  `prep.py` / `gt.py` / `masks.mjs` の3本に限る。
  実験の使い捨て出力は**スクラッチパッドに書き、`tools/out` に混ぜない**。
  一度これを破って `.c1` `.d1` `.e1` `.w_a` `.base` … が同じ階層に溜まり、
  **正式な `.mask` が一番古く使い捨てが一番新しい**という逆転が起きた。
  どれが現手法か分からなくなり、比較画像は2世代前の結果を映していた。
- **ディレクトリ名は split 名のまま。** `all_val` のような別名を作ると
  この手順書と食い違う。
- `compare.py` は `.mask` が `dist/skyMask.js` より古ければ止まる。
  古い結果を現手法だと思って見る事故は、規則より機械で止める。
- `steps.mjs` は skyMask.ts を触らず公開関数を同じ順で呼び直すが、
  最後に `segmentSky` と1バイト単位で一致することを検証して異常終了する。
  可視化のためであっても参照実装を持たない、という原則は曲げない。

```sh
node     tools/steps.mjs tools/out/scenes cloudy01 /tmp/s1 '曇天'
python3  tools/steps.py  tools/out/steps /tmp/s1 ...
```

評価は**出荷している `dist/skyMask.js` をそのまま走らせる**。
参照実装を別に持つと実装ドリフトが起きる。

比較したい設定は `tools/eval.mjs` の引数に JSON で渡す。
変更前の挙動に戻せるオプション（`modeRounds: 0`、`keepTol: 1` など）を
残しておくと、前後比較が1コマンドで済む。

```sh
node tools/eval.mjs tools/out/scenes '{}' '{"modeRounds":0}'
```
